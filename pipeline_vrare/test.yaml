DATA: 
    GENOME:          "./genome_file.fasta"   #genome (fasta file)
    SAMPLE:          "./reads_file.fastq"    #long reads (a fastq file)
    WORK_DIRECTORY:  "./directory_work"      #name of output directory
    TE_DB:           "./database_TE.fasta"   #Database of TE (a fasta file)


LOCAL_ASSEMBLY:
    FLYE:  False
    WTDGB: True

params:
    MINIMAP2:
        PRESET_OPTION: 'map-ont' # -x minimap2 preset option is map-pb by default (map-pb, map-ont etc)
        OPTION: '-t 8'
    FLYE:
        OPTIONS: ''